# Kopieer naar .env (nooit committen met echte waarden)
DATABASE_URL=postgres://bitewise:bitewise@localhost:5432/bitewise2
PORT=3001
WEB_DIST=../web/dist
# Alleen voor lokaal HTTP-gebruik: COOKIE_SECURE=0 (nooit in productie).
# Standaard zijn sessie- en CSRF-cookies Secure en vereisen HTTPS.
